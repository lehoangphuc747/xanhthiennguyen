---
import Layout from './Layout.astro';

interface Props {
  title?: string;
}

const { title = 'Hành Trình - Xanh Đi' } = Astro.props;
---

<Layout title={title}>
  <div class="min-h-screen flex flex-col">
    <Header />
    
    <main class="flex-grow bg-gradient-to-br from-background via-secondary/5 to-primary/5 pt-24">
      <slot />
    </main>

    <Footer />
  </div>
</Layout>
